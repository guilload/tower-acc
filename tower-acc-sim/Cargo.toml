[package]
name = "tower-acc-sim"
version = "0.1.0"
edition = "2024"
rust-version = "1.85"
publish = false

[dependencies]
tower-acc = { path = ".." }
axum = "0.8"
tokio = { version = "1", features = ["full"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
rand = "0.9"
